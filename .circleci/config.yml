# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
  say-hello:
    # Specify the execution environment. You can specify an image from Dockerhub or use one of our Convenience Images from CircleCI's Developer Hub.
    # See: https://circleci.com/docs/2.0/configuration-reference/#docker-machine-macos-windows-executor
    docker:
      - image: cimg/base:stable
    # Add steps to the job
    # See: https://circleci.com/docs/2.0/configuration-reference/#steps
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: sudo apt-get install java-common default-jre-headless ffmpeg
      - run:
          name: Blender
          command: apt update
      - run:
          name: Library
          command: apt remove libtcmalloc-minimal4
      - run:
          name: Library
          command: apt install libtcmalloc-minimal4
      - run:
          name: Library
          command: apt install libboost-all-dev
      - run:
          name: Library
          command: apt install libgl1-mesa-dev
      - run:
          name: Library
          command: apt install libglu1-mesa libsm-dev
      - run:
          name: Library
          command: wget https://www.sheepit-renderfarm.com/media/applet/sheepit-client-6.21338.0.jar
      - run:
          name: Render
          command: java -jar sheepit-client-6.21338.0.jar -ui oneLine/text -compute-method CPU -login lmsenavirathne -password 4Bxka7ytkU0qqunxhCMGUgtb1JU5phIgcrd0bC28 -ui text

        
      

# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  say-hello-workflow:
    jobs:
      - say-hello
